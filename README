has Matrix classes (scala idiomatic and performant/concurrent versions) with operations like cofactor, minor, determinant, inverse, etc
and linear / logistic regression and neural network functions
functions for reading octave data files
concurrency features with actor and java future implementations 
profiling aspect


